# M7 — Tournaments

Repo: /home/user/FeltSide. Constitution rules as always. Re-read
docs/specs/TOURNAMENTS.md in full — it is the spec; this is the
checklist. Save as prompts/M7.md.

## Gate 0
Verify M6 complete (its acceptance criteria; tests green). If not —
stop and report.

## Scope
1. **Presets:** schema per TOURNAMENTS §1, CRUD (coach), the four
   seeded presets of §2 with generated ladders; §4.7 export catalog
   goes real (was an empty catalog since M3).
2. **Lifecycle** (§3): scheduled → registering → running → completed;
   registration debits bankroll (type tournament_buy_in); auto-start
   at min players; level timer with breaks; BB ante from configured
   level; late-reg and re-entry windows by level; add-on at break;
   auto-elimination at stack 0 with re-entry offer; simultaneous-bust
   ordering by hand-start stacks; clock persistence per RUNTIME §1
   (restart loses ≤ one hand, never the clock).
3. **CRM-pushed tournaments:** type=tournament entries (stored inert
   since M3) activate: registration opens ahead of scheduled_start,
   soft-restricted to mapped students, preset applied.
4. **Multi-table** (§4): auto-balance on ≥2 size gap moving the
   BB-due player, stack-agnostic; dead-button rules on moves, between
   hands only; table breaking with full random redraw; final-table
   redraw; coach manual move anytime; per-tournament auto-balance
   disable.
5. **Hand-for-hand** at one-from-the-money: synchronized hand starts
   across all tables until the bubble bursts.
6. **Payouts** (§5): closed economy — prize pool from entries, standard
   table by field size, remainder to first, credited automatically
   (type tournament_payout). End-early pays by chip-count ranking.
7. **ICM** (§7): implement ICM fresh (Malmuth-Harville) with published
   test vectors logged in decisions; per-preset overlay flag (live $EV
   per stack); deal proposal at final table — coach triggers,
   unanimous accept → ICM payouts and completion.
8. **Coach interventions** (§6): pause/resume (freezes level clock +
   all action timers), advance/extend level, manual eliminate,
   registration overrides — soft-limit philosophy.
9. **Client:** tournament lobby (structure, registered list,
   clock/level, standings + eliminations), player table view with
   tournament top-bar (level, blinds/ante, timer, players left, avg
   stack), coach control panel + fast table switching (NOT a grid).
   Absence rules per TOURNAMENTS §8 (blinds post, timer folds, seat
   never vacated).
10. **Export:** finish_position + tournament session fields go real
    (CONTRACT §4.3).

## Acceptance
Full simulated 18-player, 3-table tournament driven by scripted bots
(test-only actors): balancing invariants hold at every hand boundary
(max-min ≤ 1 after moves), break/redraw correct, hand-for-hand engages
and releases, payouts reconcile to the chip (pool == entries;
sum(payouts) == pool; every ledger row typed correctly); ICM vectors
pass; clock survives forced restart; end-early pays by stacks; CRM
push→activation E2E. All prior suites green.

## Out
Time banks, bounties, satellites, multi-day, playlist injection into
tournaments (far-future per PRD), grid view.

## Discipline
As always. TOURNAMENTS.md wins every argument.
