# M6 — Review, Replay & Branch

Repo: /home/user/FeltSide. Constitution: docs/PRD.md + docs/specs/*;
spec > prompt > preference; unresolved question → stop and ask Jo.
Save as prompts/M6.md.

## Gate 0
1. Verify M4-M5 complete (prompts/M4-M5.md acceptance criteria; coached
   mode + analyzers live; tests green). If not — stop, report, do not
   start M6.
2. Verify GitHub remote holds full history; push anything unpushed.

## Scope (PRD §5)
1. **ReplayEngine graduation.** Audit legacy/game/ReplayEngine.js per
   PRD §9: fix ARCH-05 (mutation-by-reference; unbounded deep-copy on
   branch) — immutable step model or bounded snapshots. New tests from
   spec. Graduate or rebuild; log the verdict.
2. **Review page** at the reserved route `/review/<hand_id>` (M3):
   full hand replay — action-by-action forward/back, jump-to-seq,
   street jumps, board/stacks/pot reconstructed at every step. All
   hole cards visible (post-hoc review is open-kimono). Tags shown at
   their action_seq; clicking a tag jumps to it.
3. **Annotations:** coach-only text notes pinned to an action index;
   visible to everyone in the review; engine-side only (NOT exported —
   CONTRACT has no annotation field; do not add one).
4. **Retag in review** (coach): add/remove coach tags, dismiss auto
   tags. Any change bumps `revision` and re-stamps export_seq so the
   hand re-enters the export stream (plumbing live since M3). Test the
   full round trip.
5. **Branch-to-live:** from any replay point, fork into live play —
   present participants act from the reconstructed state. Branch hands
   record with origin='replay_branch', analyzers fire, bankroll never
   involved. Unbranch returns to the replay point. Chip conservation
   within the branch asserted.
6. **Group transition** (coached tables): coach's "Go to Review" moves
   every connected player at the table into the same review session
   (spectator technical state); synced navigation; "Back to Play"
   returns everyone; must work independently across parallel tables.
7. **Hand history browser** (in-engine, coach): filterable list —
   table/session, player, tag, origin, date → opens review. This is
   how descriptors earn their keep (playlist building starts here:
   "save hand as scenario" hook from review, reusing M4's
   save-as-scenario).

## Acceptance
Replay reconstruction property test (replaying recorded actions
reproduces stored stack_end/pot for 50 random recorded hands); revision
round-trip test (retag → bump → hand re-exported once, CRM-side dedup
semantics preserved — no export guarantee weakened); branch E2E with
chip conservation; group transition E2E with two parallel tables; all
prior suites green.

## Out
Tournaments (M7), weighted ranges, annotation export, retroactive
re-analysis, multi-table grid.

## Discipline
Deviations → docs/decisions/. Append-only migrations. Fix
implementations, never tests.
