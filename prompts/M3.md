# M3 — Export API, Lesson Sync & First Deployment

Re-read docs/PRD.md and docs/specs/CONTRACT.md fully. CONTRACT.md is the
single binding source for everything in this milestone — implement it,
don't reinterpret it. Save this file as prompts/M3.md.

## Step 0 — Debts
1. The private GitHub repo FeltSide now exists (Jo created it). Add
   remote, push full history, verify, then delete
   claude/feltside-bootstrap-setup-6ba75x from poker-trainer. If the repo
   STILL doesn't exist — stop and tell Jo before any M3 work.
2. Amend docs/PRD.md §9 per decision 0004: "must-carry gold" →
   RangeMatrix/RangePicker are BUILT NEW in M4; legacy/extracted/* is raw
   material only. Log as part of 0004, keep the constitution truthful.

## Scope (PRD §11 M3): the entire CONTRACT surface + Fly deployment
The CRM's EnginePoller was built against a mock of this contract. After
this milestone it points at a real engine.

### 1. Export API — /export/v1/
- Auth: ONE static API key from env (`EXPORT_API_KEY`), Bearer. 401 with
  code invalid_api_key otherwise. Never log the key.
- **meta**: engine_version, contract_version 1, tag_vocabulary_version 1,
  and the FULL tag list from docs/specs/TAXONOMY.md §§1–4 (tag, tag_type,
  description). Build it as a static vocabulary module that M5 analyzers
  will import — one source of truth from day one.
- **players / sessions / hands / playlists / tournament-presets**: exact
  CONTRACT shapes. playlists and tournament-presets return valid empty
  catalogs until M4/M7. hands export with `tags: []` until M5 — every
  other field real, including origin, booleans, review_url.
- **review_url**: format locked as `https://<host>/review/<hand_id>`.
  Reserve the client route now (placeholder page); M6 fills it. URLs
  must never change shape later.
- **Cursor design** (CONTRACT §3 guarantees are the requirement, the
  mechanism is yours — recommended: an `export_seq` BIGSERIAL stamped on
  session/hand finalize gives total order for free; cursor = opaque
  base64 of last seq). MUST hold: total stable ordering, resume never
  skips, at-least-once tolerated, 400 invalid_cursor on garbage,
  page-limit max 500.
- **revision**: hands get `revision INT DEFAULT 1` now; a bump re-stamps
  export_seq so the hand re-enters the stream. M6 triggers bumps; the
  plumbing and its tests land here.

### 2. Lesson sync — PUT /sync/v1/lessons (CONTRACT §8)
- Same API key. Declarative reconcile of the full pushed horizon:
  create/update/remove SCHEDULED entries; NEVER touch a table whose
  session started; entries absent from the snapshot and still scheduled
  are removed.
- type=lesson → scheduled coached table bound to crm_entry_id, playlist
  reference stored (inert until M4), soft seat list stored.
- type=tournament → scheduled tournament row stored inert until M7
  (preset reference kept even though presets don't exist yet).
- Scheduled entries appear in the lobby with their start time; joining
  is blocked with "opens at lesson time" until M4 activates coached
  mode.
- Unmapped student_crm_ids stored and surfaced (lobby badge), never
  blocking.

### 3. Fly.io deployment
- fly.toml + Dockerfile; secrets via fly secrets (DB URL, JWT secret,
  EXPORT_API_KEY, coach seed). Region closest to Israel.
- RUNTIME §1 guard: the idle shutdown path may run ONLY with zero active
  tables (scheduled-only counts as inactive). Scale-to-zero stays on.
- docs/DEPLOY.md: exact first-deploy and redeploy commands for Jo.
- Deploy, verify live: /health, authed /export/v1/meta, one full
  cursor walk over the M2 test data.

## Acceptance criteria
1. Contract-compliance suite: every endpoint's shape validated against
   CONTRACT literally; cursor tests prove no-skip across restarts,
   at-least-once on overlap, invalid_cursor reset behavior; 401 paths.
2. Sync reconcile suite: add / update / cancel / never-touch-started /
   all-entries-removed; idempotent double-push proves no duplicates.
3. Live on Fly: cold start < 15s to first byte on /export/v1/meta
   (CONTRACT §2 tolerance), verified after forced scale-to-zero.
4. Root npm test green; no regression in M1/M2 suites.

## Out of M3
Analyzers/tags (M5), dealing & coached activation (M4), review page
content (M6), tournament behavior (M7), any CRM-side code.

## Discipline
Deviations → docs/decisions/. Stop-and-ask beats guessing. Append-only
migrations. Never weaken a CONTRACT guarantee to pass a test —
fix the implementation.
