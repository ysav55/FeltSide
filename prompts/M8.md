# M8 — Production Hardening & Handoff
# (defined post-PRD by command decision — amend PRD §11 to include it)

Repo: /home/user/FeltSide (+ read access to epoker-crm if present, for
the final E2E only). Save as prompts/M8.md; add M8 to docs/PRD.md §11.

## Gate 0
Verify M7 complete. If not — stop and report.

## Scope
1. **Legacy retirement:** legacy/ must be EMPTY — every remaining file
   either graduated with tests or deleted with a decision entry
   explaining why it was never needed. Delete the folder; PRD §9
   updated to past tense. `git grep -i legacy` returns only docs.
2. **Load & soak:** scripted-actor load test at target scale — 6
   concurrent tables (mixed: tournament 3 tables + coached + 2
   uncoached), 45+ minutes, on the Fly instance sizing. Measure action
   latency p95, memory ceiling, and verify zero chip-conservation or
   ledger-invariant violations under load. If 512MB doesn't hold it,
   recommend sizing with numbers.
3. **Crash drills:** forced kill during — tournament level change,
   awaiting_deal, showdown, mid-export cursor walk, mid-sync reconcile.
   Every drill: recovery per RUNTIME §1, ledger reconciles, export
   guarantees hold (no skip, at-least-once).
4. **Security pass:** rate limiting on /auth/*; export/sync endpoints
   reject non-key traffic with no information leak; dependency audit;
   secrets never logged (grep the logger); JWT expiry + re-auth flow
   sane; document EXPORT_API_KEY rotation procedure.
5. **Operations:** DB backup strategy documented + restore drill
   actually performed once; Fly health checks + alerting on crash
   loops; structured error logging with an operator-readable tail
   command; docs/RUNBOOK.md for Jo — deploy, redeploy, rollback,
   backup/restore, key rotation, "student can't log in" checklist,
   "tournament stuck" checklist. Phone-readable.
6. **Conformance audit:** walk PRD + all five specs section by section
   against the code; every gap either fixed or logged as a numbered
   known-gap with severity in docs/decisions/. The constitution must
   end truthful.
7. **Final grand E2E** (with CRM if available): one evening simulated —
   lesson sync creates a coached table, lesson plays with drills, a
   pushed tournament runs to payout, uncoached homework session,
   review with retag, CRM poll lands everything with correct
   observations and zero duplicates. Human-readable run log for Jo.

## Acceptance
All drills documented with outcomes; runbook complete; legacy/ gone;
conformance audit delivered; grand E2E log delivered; all suites green.

## Out
New features of any kind. M8 hardens; it does not extend.

## Discipline
As always. This milestone's deliverable is TRUST, in writing.
